# testkube/

**Intentionally empty.** TestWorkflows are built separately, by hand.

This folder exists so the repo's intent is clear: the application + raw test files live here; the TestKube TestWorkflows, orchestrator, condition/execute branching, composite workflow, and any control-plane wiring all live and are authored elsewhere.

If anything other than this README appears in this folder, something has drifted out of scope.
